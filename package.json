{
  "name": "appscan-dast-action",
  "version": "1.0.8",
  "description": "HCL AppScan DAST GitHub Action",
  "main": "dist/main.js",
  "scripts": {
    "build": "ncc build main.js -o dist/main --no-cache && ncc build cancelJob.js -o dist/cancelJob --no-cache",
    "prepack": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
